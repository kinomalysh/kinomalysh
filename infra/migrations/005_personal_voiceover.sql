-- Персональная озвучка: если в тексте сцены есть плейсхолдер имени,
-- реплика синтезируется под конкретного ребёнка и хранится по заказу.
-- Идемпотентно, применять напрямую psql (см. infra/README).

ALTER TABLE story_scenes ADD COLUMN IF NOT EXISTS vo_key text;
